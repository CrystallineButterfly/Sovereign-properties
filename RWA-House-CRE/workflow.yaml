# ==========================================================================
# CRE WORKFLOW SETTINGS FILE
# ==========================================================================

anvil-settings:
  user-workflow:
    workflow-name: "RWA-House-CRE-anvil"
  workflow-artifacts:
    workflow-path: "."
    config-path: "./config.anvil.json"
    secrets-path: "../secrets.yaml"

staging-settings:
  user-workflow:
    workflow-name: "RWA-House-CRE-staging"
  workflow-artifacts:
    workflow-path: "."
    config-path: "./config.staging.json"
    secrets-path: "../secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "RWA-House-CRE-production"
  workflow-artifacts:
    workflow-path: "."
    config-path: "./config.production.json"
    secrets-path: "../secrets.yaml"
